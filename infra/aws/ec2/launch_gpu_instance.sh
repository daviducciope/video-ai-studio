#!/usr/bin/env bash
set -euo pipefail

echo "Template only: questo script non deve essere eseguito in questo step."
echo "Preparare prima subnet, security group, IAM instance profile e budget guardrails."
exit 1
